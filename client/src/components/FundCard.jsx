import React from "react";

const FundCard = ({
  owner,
  title,
  description,
  target,
  deadline,
  amountCollected,
  image,
  handleClick,
}) => {
  return <div>FundCard</div>;
};

export default FundCard;
